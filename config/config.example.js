// Create a copy of this file named config.js in this directory
// Add the needed credentials
// Do not commit config.js

const config = {};

// https://help.github.com/en/articles/creating-a-personal-access-token-for-the-command-line
// needs repo access I think
config.githubApiToken = 'YOUR TOKEN HERE';

module.exports = config;
